# @package _global_

defaults:
  - override /datamodule: de_selfsupervised_2d
  - override /model: de_selfsupervised_2d
  - override /callbacks: default
  - override /logger: wandb
  - override /trainer: nnunet
  - override /hydra: nnunet

tags: ["${model.name}_${nnUNet_variant}", "Fold${fold}"]
task_name: "DE"
fold: 0
train: True
test: True
nnUNet_variant: 2D
best_model: False
logger:
  wandb:
    project: "${task_name}"
    name: "${model.name}_${nnUNet_variant}"
    tags: ["Fold${fold}"]
