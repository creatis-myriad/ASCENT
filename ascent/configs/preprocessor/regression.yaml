_target_: ascent.preprocessing.preprocessing.RegPreprocessor
dataset_path: ${paths.data_dir}/${dataset}
do_resample: ${resample}
do_normalize: ${normalize}
num_workers: 12
overwrite_existing: ${overwrite}
anisotropy_threshold: 3
verbose: ${verbose}
